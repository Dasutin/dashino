# Server/Jobs env
PORT=4040
# Example secrets (replace with real tokens locally; do not commit real values)
HA_BASE_URL=http://homeassistant.local:8123
HA_TOKEN=REPLACE_ME

# Disable server-side caches for dashboards/widgets/themes (for debugging only)
CACHE_DISABLE=false

# Optional client build-time API origin override
VITE_API_ORIGIN=http://localhost:4040
